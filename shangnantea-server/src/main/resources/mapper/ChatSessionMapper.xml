<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shangnantea.mapper.ChatSessionMapper">
    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.shangnantea.model.entity.message.ChatSession">
        <id column="id" property="id"/>
        <result column="initiator_id" property="initiatorId"/>
        <result column="receiver_id" property="receiverId"/>
        <result column="last_message" property="lastMessage"/>
        <result column="last_message_time" property="lastMessageTime"/>
        <result column="initiator_unread" property="initiatorUnread"/>
        <result column="receiver_unread" property="receiverUnread"/>
        <result column="is_pinned" property="isPinned"/>
        <result column="session_type" property="sessionType"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 所有列 -->
    <sql id="Base_Column_List">
        id, initiator_id, receiver_id, last_message, last_message_time, initiator_unread, receiver_unread, is_pinned, session_type, status, create_time, update_time
    </sql>

    <!-- 插入记录 -->
    <insert id="insert" parameterType="com.shangnantea.model.entity.message.ChatSession">
        INSERT INTO chat_sessions (
            id, initiator_id, receiver_id, last_message, last_message_time, initiator_unread, receiver_unread, is_pinned, session_type, status, create_time, update_time
        ) VALUES (
            #{id}, #{initiatorId}, #{receiverId}, #{lastMessage}, #{lastMessageTime}, #{initiatorUnread}, #{receiverUnread}, #{isPinned}, #{sessionType}, #{status}, #{createTime}, #{updateTime}
        )
    </insert>

    <!-- 根据主键删除记录 -->
    <delete id="deleteById" parameterType="java.lang.String">
        DELETE FROM chat_sessions WHERE id = #{id}
    </delete>

    <!-- 根据主键批量删除记录 -->
    <delete id="deleteByIds" parameterType="java.util.List">
        DELETE FROM chat_sessions WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 根据主键更新记录 -->
    <update id="updateById" parameterType="com.shangnantea.model.entity.message.ChatSession">
        UPDATE chat_sessions
        <set>
            <if test="initiatorId != null">initiator_id = #{initiatorId},</if>
            <if test="receiverId != null">receiver_id = #{receiverId},</if>
            <if test="lastMessage != null">last_message = #{lastMessage},</if>
            <if test="lastMessageTime != null">last_message_time = #{lastMessageTime},</if>
            <if test="initiatorUnread != null">initiator_unread = #{initiatorUnread},</if>
            <if test="receiverUnread != null">receiver_unread = #{receiverUnread},</if>
            <if test="isPinned != null">is_pinned = #{isPinned},</if>
            <if test="sessionType != null">session_type = #{sessionType},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 根据主键查询记录 -->
    <select id="selectById" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM chat_sessions
        WHERE id = #{id}
    </select>

    <!-- 根据主键批量查询记录 -->
    <select id="selectByIds" parameterType="java.util.List" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM chat_sessions
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 查询所有记录 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM chat_sessions
    </select>

    <!-- 统计记录总数 -->
    <select id="count" resultType="java.lang.Long">
        SELECT COUNT(*) FROM chat_sessions
    </select>

    <!-- 根据用户ID查询会话列表 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM chat_sessions
        WHERE (initiator_id = #{userId} OR receiver_id = #{userId})
        AND status = 1
        ORDER BY is_pinned DESC, last_message_time DESC
    </select>

    <!-- 根据两个用户ID查询会话 -->
    <select id="selectByUserIds" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM chat_sessions
        WHERE ((initiator_id = #{userId1} AND receiver_id = #{userId2})
           OR (initiator_id = #{userId2} AND receiver_id = #{userId1}))
        AND status = 1
        LIMIT 1
    </select>
</mapper> 