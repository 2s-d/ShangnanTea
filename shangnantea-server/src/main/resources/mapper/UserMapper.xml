<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shangnantea.mapper.UserMapper">
    
    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.shangnantea.model.entity.user.User">
        <id column="id" property="id" jdbcType="VARCHAR"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="nickname" property="nickname" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="avatar" property="avatar" jdbcType="VARCHAR"/>
        <result column="role" property="role" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="is_deleted" property="isDeleted" jdbcType="INTEGER"/>
        <result column="gender" property="gender" jdbcType="INTEGER"/>
        <result column="birthday" property="birthday" jdbcType="DATE"/>
        <result column="bio" property="bio" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>
    
    <!-- 公共列 -->
    <sql id="Base_Column_List">
        id, username, password, nickname, phone, email, avatar, role, status, is_deleted, 
        gender, birthday, bio, create_time, update_time
    </sql>
    
    <!-- 通过ID查询用户 -->
    <select id="selectById" resultMap="BaseResultMap" parameterType="java.lang.String">
        SELECT 
        <include refid="Base_Column_List"/>
        FROM users
        WHERE id = #{id,jdbcType=VARCHAR} AND is_deleted = 0
    </select>
    
    <!-- 通过用户名查询用户 -->
    <select id="selectByUsername" resultMap="BaseResultMap" parameterType="java.lang.String">
        SELECT 
        <include refid="Base_Column_List"/>
        FROM users
        WHERE username = #{username,jdbcType=VARCHAR} AND is_deleted = 0
    </select>
    
    <!-- 插入用户 -->
    <insert id="insert" parameterType="com.shangnantea.model.entity.user.User">
        INSERT INTO users (
            id, username, password, nickname, phone, email, avatar, 
            role, status, is_deleted, gender, birthday, bio, create_time, update_time
        ) VALUES (
            #{id,jdbcType=VARCHAR}, 
            #{username,jdbcType=VARCHAR}, 
            #{password,jdbcType=VARCHAR}, 
            #{nickname,jdbcType=VARCHAR}, 
            #{phone,jdbcType=VARCHAR}, 
            #{email,jdbcType=VARCHAR}, 
            #{avatar,jdbcType=VARCHAR}, 
            #{role,jdbcType=INTEGER}, 
            #{status,jdbcType=INTEGER}, 
            #{isDeleted,jdbcType=INTEGER}, 
            #{gender,jdbcType=INTEGER}, 
            #{birthday,jdbcType=DATE}, 
            #{bio,jdbcType=VARCHAR}, 
            #{createTime,jdbcType=TIMESTAMP}, 
            #{updateTime,jdbcType=TIMESTAMP}
        )
    </insert>
    
    <!-- 更新用户 -->
    <update id="update" parameterType="com.shangnantea.model.entity.user.User">
        UPDATE users
        <set>
            <if test="username != null">username = #{username,jdbcType=VARCHAR},</if>
            <if test="password != null">password = #{password,jdbcType=VARCHAR},</if>
            <if test="nickname != null">nickname = #{nickname,jdbcType=VARCHAR},</if>
            <if test="phone != null">phone = #{phone,jdbcType=VARCHAR},</if>
            <if test="email != null">email = #{email,jdbcType=VARCHAR},</if>
            <if test="avatar != null">avatar = #{avatar,jdbcType=VARCHAR},</if>
            <if test="role != null">role = #{role,jdbcType=INTEGER},</if>
            <if test="status != null">status = #{status,jdbcType=INTEGER},</if>
            <if test="isDeleted != null">is_deleted = #{isDeleted,jdbcType=INTEGER},</if>
            <if test="gender != null">gender = #{gender,jdbcType=INTEGER},</if>
            <if test="birthday != null">birthday = #{birthday,jdbcType=DATE},</if>
            <if test="bio != null">bio = #{bio,jdbcType=VARCHAR},</if>
            update_time = now()
        </set>
        WHERE id = #{id,jdbcType=VARCHAR}
    </update>
    
    <!-- 更新密码 -->
    <update id="updatePassword">
        UPDATE users
        SET password = #{password,jdbcType=VARCHAR},
        update_time = now()
        WHERE id = #{id,jdbcType=VARCHAR}
    </update>
    
    <!-- 更新头像 -->
    <update id="updateAvatar">
        UPDATE users
        SET avatar = #{avatar,jdbcType=VARCHAR},
        update_time = now()
        WHERE id = #{id,jdbcType=VARCHAR}
    </update>
    
    <!-- 删除用户 -->
    <delete id="delete" parameterType="java.lang.String">
        UPDATE users
        SET is_deleted = 1,
        update_time = now()
        WHERE id = #{id,jdbcType=VARCHAR}
    </delete>
</mapper> 