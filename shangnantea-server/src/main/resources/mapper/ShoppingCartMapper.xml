<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shangnantea.mapper.ShoppingCartMapper">
    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.shangnantea.model.entity.order.ShoppingCart">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="tea_id" property="teaId"/>
        <result column="spec_id" property="specId"/>
        <result column="quantity" property="quantity"/>
        <result column="selected" property="selected"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 所有列 -->
    <sql id="Base_Column_List">
        id, user_id, tea_id, spec_id, quantity, selected, create_time, update_time
    </sql>

    <!-- 插入记录 -->
    <insert id="insert" parameterType="com.shangnantea.model.entity.order.ShoppingCart">
        INSERT INTO shopping_cart (
            id, user_id, tea_id, spec_id, quantity, selected, create_time, update_time
        ) VALUES (
            #{id}, #{userId}, #{teaId}, #{specId}, #{quantity}, #{selected}, #{createTime}, #{updateTime}
        )
    </insert>

    <!-- 根据主键删除记录 -->
    <delete id="deleteById" parameterType="java.lang.Integer">
        DELETE FROM shopping_cart WHERE id = #{id}
    </delete>

    <!-- 根据主键批量删除记录 -->
    <delete id="deleteByIds" parameterType="java.util.List">
        DELETE FROM shopping_cart WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 根据主键更新记录 -->
    <update id="updateById" parameterType="com.shangnantea.model.entity.order.ShoppingCart">
        UPDATE shopping_cart
        <set>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="teaId != null">tea_id = #{teaId},</if>
            <if test="specId != null">spec_id = #{specId},</if>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="selected != null">selected = #{selected},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 根据主键查询记录 -->
    <select id="selectById" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM shopping_cart
        WHERE id = #{id}
    </select>

    <!-- 根据主键批量查询记录 -->
    <select id="selectByIds" parameterType="java.util.List" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM shopping_cart
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 查询所有记录 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM shopping_cart
    </select>

    <!-- 统计记录总数 -->
    <select id="count" resultType="java.lang.Long">
        SELECT COUNT(*) FROM shopping_cart
    </select>

    <!-- 根据用户ID查询购物车列表 -->
    <select id="selectByUserId" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM shopping_cart
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>
    
    <!-- 根据用户ID、茶叶ID和规格ID查询购物车项 -->
    <select id="selectByUserIdAndTeaIdAndSpecId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM shopping_cart
        WHERE user_id = #{userId}
        AND tea_id = #{teaId}
        <choose>
            <when test="specId != null">
                AND spec_id = #{specId}
            </when>
            <otherwise>
                AND spec_id IS NULL
            </otherwise>
        </choose>
        LIMIT 1
    </select>
</mapper> 