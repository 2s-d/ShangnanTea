<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shangnantea.mapper.ShopCertificationMapper">
    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.shangnantea.model.entity.shop.ShopCertification">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="real_name" property="realName"/>
        <result column="id_card" property="idCard"/>
        <result column="id_card_front" property="idCardFront"/>
        <result column="id_card_back" property="idCardBack"/>
        <result column="business_license" property="businessLicense"/>
        <result column="shop_name" property="shopName"/>
        <result column="contact_phone" property="contactPhone"/>
        <result column="province" property="province"/>
        <result column="city" property="city"/>
        <result column="district" property="district"/>
        <result column="address" property="address"/>
        <result column="apply_reason" property="applyReason"/>
        <result column="status" property="status"/>
        <result column="admin_id" property="adminId"/>
        <result column="reject_reason" property="rejectReason"/>
        <result column="notification_id" property="notificationId"/>
        <result column="notification_confirmed" property="notificationConfirmed"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 所有列 -->
    <sql id="Base_Column_List">
        id, user_id, real_name, id_card, id_card_front, id_card_back, business_license, 
        shop_name, contact_phone, province, city, district, address, apply_reason, status, 
        admin_id, reject_reason, notification_id, notification_confirmed, create_time, update_time
    </sql>

    <!-- 插入记录 -->
    <insert id="insert" parameterType="com.shangnantea.model.entity.shop.ShopCertification">
        INSERT INTO shop_certifications (
            id, user_id, real_name, id_card, id_card_front, id_card_back, business_license,
            shop_name, contact_phone, province, city, district, address, apply_reason, status,
            admin_id, reject_reason, notification_id, notification_confirmed, create_time, update_time
        ) VALUES (
            #{id}, #{userId}, #{realName}, #{idCard}, #{idCardFront}, #{idCardBack}, #{businessLicense},
            #{shopName}, #{contactPhone}, #{province}, #{city}, #{district}, #{address}, #{applyReason}, #{status},
            #{adminId}, #{rejectReason}, #{notificationId}, #{notificationConfirmed}, #{createTime}, #{updateTime}
        )
    </insert>

    <!-- 根据主键删除记录 -->
    <delete id="deleteById" parameterType="java.lang.Integer">
        DELETE FROM shop_certifications WHERE id = #{id}
    </delete>

    <!-- 根据主键批量删除记录 -->
    <delete id="deleteByIds" parameterType="java.util.List">
        DELETE FROM shop_certifications WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 根据主键更新记录 -->
    <update id="updateById" parameterType="com.shangnantea.model.entity.shop.ShopCertification">
        UPDATE shop_certifications
        <set>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="idCard != null">id_card = #{idCard},</if>
            <if test="idCardFront != null">id_card_front = #{idCardFront},</if>
            <if test="idCardBack != null">id_card_back = #{idCardBack},</if>
            <if test="businessLicense != null">business_license = #{businessLicense},</if>
            <if test="shopName != null">shop_name = #{shopName},</if>
            <if test="contactPhone != null">contact_phone = #{contactPhone},</if>
            <if test="province != null">province = #{province},</if>
            <if test="city != null">city = #{city},</if>
            <if test="district != null">district = #{district},</if>
            <if test="address != null">address = #{address},</if>
            <if test="applyReason != null">apply_reason = #{applyReason},</if>
            <if test="status != null">status = #{status},</if>
            <if test="adminId != null">admin_id = #{adminId},</if>
            <if test="rejectReason != null">reject_reason = #{rejectReason},</if>
            <if test="notificationId != null">notification_id = #{notificationId},</if>
            <if test="notificationConfirmed != null">notification_confirmed = #{notificationConfirmed},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 根据主键查询记录 -->
    <select id="selectById" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM shop_certifications
        WHERE id = #{id}
    </select>

    <!-- 根据主键批量查询记录 -->
    <select id="selectByIds" parameterType="java.util.List" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM shop_certifications
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 查询所有记录 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM shop_certifications
    </select>

    <!-- 统计记录总数 -->
    <select id="count" resultType="java.lang.Long">
        SELECT COUNT(*) FROM shop_certifications
    </select>
    
    <!-- 根据用户ID查询认证信息 -->
    <select id="selectByUserId" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM shop_certifications
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
        LIMIT 1
    </select>
</mapper> 