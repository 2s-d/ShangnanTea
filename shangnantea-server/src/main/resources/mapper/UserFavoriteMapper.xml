<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shangnantea.mapper.UserFavoriteMapper">
    
    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.shangnantea.model.entity.user.UserFavorite">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="user_id" property="userId" jdbcType="VARCHAR"/>
        <result column="item_type" property="itemType" jdbcType="VARCHAR"/>
        <result column="item_id" property="itemId" jdbcType="VARCHAR"/>
        <result column="target_name" property="targetName" jdbcType="VARCHAR"/>
        <result column="target_image" property="targetImage" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
    </resultMap>
    
    <!-- 公共列 -->
    <sql id="Base_Column_List">
        id, user_id, item_type, item_id, target_name, target_image, create_time
    </sql>
    
    <!-- 根据ID查询收藏 -->
    <select id="selectById" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        SELECT 
        <include refid="Base_Column_List"/>
        FROM user_favorites
        WHERE id = #{id,jdbcType=INTEGER}
    </select>
    
    <!-- 根据用户ID查询收藏列表 -->
    <select id="selectByUserId" resultMap="BaseResultMap" parameterType="java.lang.String">
        SELECT 
        <include refid="Base_Column_List"/>
        FROM user_favorites
        WHERE user_id = #{userId,jdbcType=VARCHAR}
        ORDER BY create_time DESC
    </select>
    
    <!-- 根据用户ID和收藏类型查询收藏列表 -->
    <select id="selectByUserIdAndType" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List"/>
        FROM user_favorites
        WHERE user_id = #{userId,jdbcType=VARCHAR} AND item_type = #{itemType,jdbcType=VARCHAR}
        ORDER BY create_time DESC
    </select>
    
    <!-- 检查是否已收藏 -->
    <select id="selectByUserIdAndItem" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List"/>
        FROM user_favorites
        WHERE user_id = #{userId,jdbcType=VARCHAR} AND item_type = #{itemType,jdbcType=VARCHAR} AND item_id = #{itemId,jdbcType=VARCHAR}
        LIMIT 1
    </select>
    
    <!-- 插入收藏 -->
    <insert id="insert" parameterType="com.shangnantea.model.entity.user.UserFavorite" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_favorites (
            user_id, item_type, item_id, target_name, target_image, create_time
        ) VALUES (
            #{userId,jdbcType=VARCHAR}, 
            #{itemType,jdbcType=VARCHAR}, 
            #{itemId,jdbcType=VARCHAR}, 
            #{targetName,jdbcType=VARCHAR}, 
            #{targetImage,jdbcType=VARCHAR}, 
            #{createTime,jdbcType=TIMESTAMP}
        )
    </insert>
    
    <!-- 删除收藏 -->
    <delete id="deleteById" parameterType="java.lang.Integer">
        DELETE FROM user_favorites
        WHERE id = #{id,jdbcType=INTEGER}
    </delete>
    
    <!-- 取消收藏 -->
    <delete id="deleteByUserIdAndItem">
        DELETE FROM user_favorites
        WHERE user_id = #{userId,jdbcType=VARCHAR} AND item_type = #{itemType,jdbcType=VARCHAR} AND item_id = #{itemId,jdbcType=VARCHAR}
    </delete>
    
    <!-- 删除用户的所有收藏 -->
    <delete id="deleteByUserId" parameterType="java.lang.String">
        DELETE FROM user_favorites
        WHERE user_id = #{userId,jdbcType=VARCHAR}
    </delete>
    
    <!-- 统计用户的收藏数 -->
    <select id="countByUserId" resultType="java.lang.Long">
        SELECT COUNT(*) FROM user_favorites
        WHERE user_id = #{userId}
    </select>
</mapper> 